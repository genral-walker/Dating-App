import SwiftUI

struct ProfileView: View {
    var body: some View {
        ZStack {
            Color.black.ignoresSafeArea()
            Text("Profile View")
                .foregroundColor(.white)
                .font(.largeTitle)
        }
    }
}
