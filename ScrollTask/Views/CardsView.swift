import SwiftUI

struct CardsView: View {
    var body: some View {
        ZStack {
            Color.black.ignoresSafeArea()
            Text("Cards View")
                .foregroundColor(.white)
                .font(.largeTitle)
        }
    }
}
